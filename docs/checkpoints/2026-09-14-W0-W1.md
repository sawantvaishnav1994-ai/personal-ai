# Personal AI Continuation Checkpoint — W0/W1

Date: 2026-09-14

## Workstream

W0 authoritative baseline + W1 durable-storage and encrypted-recovery hardening.

## Git state

- Starting SHA: `a8a6d61be84747076a04360bf6f46defbeb7a664`
- First storage-guard candidate: `3a2b3709190ea1fac98c8ec4c91ddf01be824fea`
- Exact encrypted-backup validation candidate: `0988d2a77a79cebeaac13788c0a3033ed8de2106`
- Branch: `completion/continuous-product-20260914`
- Draft PR: #22, stacked on `completion/p3-p10-product-20260912`
- PR #18 and PR #21 remain unmodified, unmerged and unpromoted.

## W1 durable-storage guard

The production `personal-ai-runtime` Railway service has no persistent volume. The application previously defaulted hosted state to the container filesystem and could therefore initialize SQLite databases and owner data on ephemeral storage.

Hosted startup now validates storage before `build_runtime()` opens production stores. A hosted runtime requires:

1. `PERSONAL_AI_DATA_DIR` beneath `PERSONAL_AI_DURABLE_ROOT` (default `/data`).
2. A real non-root Linux mount containing the data directory.
3. A write + flush + `fsync` + read-back storage probe.

Failure raises `StorageUnavailable` and prevents the privileged runtime from starting on unproven ephemeral storage. Local/desktop operation remains unaffected.

## W1 encrypted backup implementation

Status for exact candidate `0988d2a77a79cebeaac13788c0a3033ed8de2106`:

- IMPLEMENTED
- INTEGRATED
- AUTOMATED VALIDATED
- NOT YET OPERATIONALLY VERIFIED
- NOT YET PRODUCTION VERIFIED

New recovery behavior includes:

- AES-256-GCM authenticated encryption for newly created `.paibackup` archives.
- Per-backup key derivation via HKDF-SHA256 from the existing owner root-key architecture.
- Root key is never embedded in the archive.
- Safe SQLite online backup snapshots rather than blind copying of active database files.
- SQLite WAL/SHM/journal sidecars excluded from backup payloads.
- Archive path traversal, symlink and unlisted-payload defenses.
- Integrity metadata checks before restore.
- Full staged database integrity validation before any owner-data file replacement begins.
- Temporary replacement + `os.replace()` per restored file.
- Read-only legacy v1 backup compatibility; new backups are never plaintext.
- Wrong-key/tamper failure tests and live-WAL snapshot coverage.

Operational verification remains required in an isolated runtime and then on actual durable Railway storage. No production mutation was performed.

## Exact-head automated evidence for `0988d2a77a79cebeaac13788c0a3033ed8de2106`

All six GitHub workflow suites associated with this exact SHA completed successfully:

- CI run `34807224081` / #475: PASS
- Reliability and Security run `34807224054` / #103: PASS
- P3 iPhone PWA run `34807224092` / #71: PASS
- Android Instrumentation run `34807224084` / #102: PASS
- Package Validation run `34807224062` / #102: PASS
- iOS Companion run `34807224085` / #84: PASS

This establishes automated validation only. It does not substitute for isolated operational restore evidence or production restart/redeploy persistence evidence.

## Deployment

- Deployment performed from PR #22: NO
- Railway production changed: NO
- Existing healthy production deployment remains `8954a483-c1c2-486e-b345-286e95fa2a2f`
- Existing deployed source remains `e63f02b653dd821ebe41cb7100dadd1b99f0af53`
- Existing live URL remains `https://personal-ai-runtime-production.up.railway.app/iphone/`
- `personal-ai-runtime`: NO persistent volume
- `personal-ai-iphone-qualification`: 500 MB volume mounted at `/data`

Deployment of PR #22 is intentionally blocked because the main runtime still has no persistent volume. Deploying the fail-closed storage guard before durable storage is attached would correctly make the service refuse startup.

## Review status

Secret-leakage and destructive-restore review is the next coordinator gate. Automated success alone does not authorize production deployment.

## Known limitations / remaining W1 gates

- Isolated operational backup/create/inspect/restore/reopen verification must pass without production mutation.
- Actual `personal-ai-runtime` state must survive a real service restart.
- Actual `personal-ai-runtime` state must survive a redeploy of the same validated candidate.
- Backup from durable runtime and isolated restore/checksum/record verification must pass.
- Production remains blocked until a persistent volume is attached and the validated data path is configured beneath it.

## Owner blockers

See `docs/OWNER_BLOCKERS.md`. The immediate conditional blocker is durable Railway storage creation/attachment if it requires billable infrastructure or a dashboard-only owner action.

## Exact next continuation point

1. Review exact PR #22 diff for secret leakage and destructive restore risks.
2. Add isolated operational backup/restore verification without production mutation.
3. Continue W2 Trusted Action Core: result-verification contracts, rollback metadata, critical-action reauthentication, dynamic risk/destination policy, audit redaction/tamper evidence, first-class PWA session binding, session-revocation tests.
4. Continue unblocked W3 automated conversation/voice/device reliability work.
5. Do not deploy PR #22 to `personal-ai-runtime` until durable storage exists and coordinator gates pass.
