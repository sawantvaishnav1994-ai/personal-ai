# Merge Rule

Do not merge the iPhone-first Phase A branch into `main` until the exact candidate SHA has passed all required readiness checks and merge is explicitly approved. Physical P3.1 qualification remains a later evidence step.
