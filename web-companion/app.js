const pages=[...document.querySelectorAll('.page')];const tabs=[...document.querySelectorAll('nav button')];tabs.forEach(t=>t.addEventListener('click',()=>{tabs.forEach(x=>x.classList.toggle('active',x===t));pages.forEach(p=>p.classList.toggle('active',p.id===t.dataset.page));}));document.getElementById('ask').addEventListener('submit',e=>{e.preventDefault();const input=document.getElementById('prompt');if(!input.value.trim())return;input.value='';input.placeholder='Secure runtime not paired yet';setTimeout(()=>input.placeholder='Ask Personal AI anything…',2200);});if('serviceWorker'in navigator){navigator.serviceWorker.register('/sw.js').catch(()=>{});}